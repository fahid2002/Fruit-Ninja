# Fruit Slice Cam

A browser-based fruit slicing game controlled by a laptop camera and fingertip tracking.

## Local Run

```bash
npm install
npm run dev
```

Open the local URL shown in the terminal. Camera access works on `localhost` and on HTTPS deployments such as Vercel.

## Build

```bash
npm run build
```

The production output is generated in `dist`.
